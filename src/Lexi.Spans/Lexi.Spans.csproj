<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<!-- Span-first prototype (spike/span-first-lexer). Not packaged, kept out of Lexi.slnx; exists to be
		     benchmarked against the shipping string-based Lexi. Faithful parallel of the real types under a
		     distinct namespace so both keep identical names. -->
		<IsPackable>false</IsPackable>
		<IsAotCompatible>true</IsAotCompatible>
	</PropertyGroup>

</Project>
